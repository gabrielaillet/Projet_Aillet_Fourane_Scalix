package scala

/**
 * Represents a director with an ID and name.
 *
 * @param id   The unique identifier of the director.
 * @param name The name of the director.
 */
case class Director(id: Int, name: String)